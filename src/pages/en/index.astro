---
import MainLayout from "../../layouts/MainLayout.astro";

import Header from "../../components/Header.astro";


// importamos el diccionario que va con {}
import { useTranslations } from "../../i18n";

// -- cambiamos el idioma a inglés --> 
const t = useTranslations("en");  
---

<MainLayout title={t("site.title")} description ={t("site.description")}>
    <Header />   <!-- como esto está ya creado le decimos que lo meta aquí directamente o es como un enlace al HEADER.ASTRO --> 
</MainLayout>